{
  "name": "fighting-game-retention-system",
  "version": "1.0.0",
  "description": "Player-respecting retention and ROI system for PlayCanvas fighting platform",
  "main": "index.js",
  "scripts": {
    "dev:client": "cd src/client && bun run dev",
    "dev:backend": "cd src/backend && bun run dev",
    "build:client": "cd src/client && bun run build",
    "build:backend": "cd src/backend && bun run build",
    "test": "bun test",
    "lint": "eslint src/ --ext .ts,.js",
    "type-check": "tsc --noEmit",
    "validate-contracts": "node scripts/validate-contracts.js"
  },
  "keywords": ["playcanvas", "fighting-game", "retention", "ethical-monetization", "player-respecting"],
  "author": "Fighting Game Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "workspaces": [
    "src/client",
    "src/backend/*"
  ]
}